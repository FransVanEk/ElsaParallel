<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">ParallelElsaV3</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
          <div class="nav-item px-3">
            <NavLink class="nav-link" href="WaitAll">
                <span class="oi oi-plus" aria-hidden="true"></span> Wait all
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="WaitAny">
                <span class="oi oi-plus" aria-hidden="true"></span> Wait any
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="continuealways">
                <span class="oi oi-plus" aria-hidden="true"></span> Continue always
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="waitanyloop">
                <span class="oi oi-plus" aria-hidden="true"></span> Wait any loop
            </NavLink>
        </div>
           <div class="nav-item px-3">
            <NavLink class="nav-link" href="waitallloop">
                <span class="oi oi-plus" aria-hidden="true"></span> Wait all loop
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
